# 📄 Resumind — AI‑помощник для анализа резюме

> Интеллектуальный инструмент для загрузки резюме, конвертации в изображение и получения детального AI‑отзыва с оценками ATS и персональными советами по улучшению.

**Онлайн-версия:** [https://puter.com/app/jsm-ai-resume-analyzing-level-up](https://puter.com/app/jsm-ai-resume-analyzing-level-up)

---

## ✨ Возможности

- 📤 **Загрузка PDF‑резюме** — простой drag & drop интерфейс
- 🖼️ **Конвертация в изображение** — автоматическая обработка PDF файлов
- 🤖 **AI‑анализ резюме** — детальный отзыв с оценками и рекомендациями
- 📊 **ATS‑оценка** — проверка соответствия системам отслеживания кандидатов
- 🌍 **Мультиязычность** — поддержка английского и русского языков
- 💾 **Облачное хранилище** — все данные сохраняются в Puter
- 📝 **Мастер создания резюме** — пошаговый визард для создания резюме
- 🎨 **Предпросмотр резюме** — несколько стилей оформления (Classic, Minimal, Modern)

---

## 🛠️ Технологический стек

### Основные технологии

- **React 19** — последняя версия React с улучшенной производительностью
- **React Router 7** — маршрутизация с поддержкой SSR
- **TypeScript** — типизированный JavaScript для надежности кода
- **Tailwind CSS** — утилитарный CSS‑фреймворк для стилизации

### Дополнительные библиотеки

- **i18next** — интернационализация (EN/RU)
- **Zustand** — легковесное управление состоянием
- **Puter.js** — интеграция с облачной платформой Puter
  - Файловое хранилище (FS)
  - Key-Value хранилище (KV)
  - AI‑аналитика через Claude
- **pdfjs-dist** — конвертация PDF в изображения
- **react-dropzone** — загрузка файлов с drag & drop

---

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** 18+ или выше
- **Yarn** 1.22+ (рекомендуется) или npm

### Установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd ai-resume-analyzer

# Установите зависимости
yarn install
# или
npm install
```

### Разработка

```bash
# Запустите dev‑сервер с HMR
yarn dev
# или
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена

```bash
# Создайте оптимизированную сборку
yarn build
# или
npm run build
```

### Запуск продакшн‑версии

```bash
# Запустите собранное приложение
yarn start
# или
npm run start
```

---

## 📜 Доступные скрипты

| Скрипт | Описание |
|--------|----------|
| `yarn dev` | Запуск dev‑сервера с hot module replacement |
| `yarn build` | Создание оптимизированной продакшн‑сборки |
| `yarn start` | Запуск собранного приложения |
| `yarn typecheck` | Генерация типов роутера и проверка TypeScript |

---

## 🌍 Локализация (i18n)

Проект поддерживает мультиязычность через `i18next` и `react-i18next`.

### Поддерживаемые языки

- 🇬🇧 **English (EN)** — английский
- 🇷🇺 **Русский (RU)** — русский

### Структура локализации

```
locales/
  ├── en.json    # Английские переводы
  └── ru.json    # Русские переводы
```

### Использование в компонентах

```tsx
import { useTranslation } from "react-i18next";

function MyComponent() {
  const { t } = useTranslation();
  
  return <h1>{t("home.title")}</h1>;
}
```

### Переключение языка

Язык переключается через компонент `LanguageSwitcher` в навбаре. Выбранный язык сохраняется в `localStorage`.

---

## 🔌 Интеграция с Puter

Проект использует возможности платформы [Puter](https://puter.com) для:

### Файловое хранилище (FS)
- Загрузка и сохранение PDF‑файлов резюме
- Хранение конвертированных изображений
- Управление файлами пользователя

### Key-Value хранилище (KV)
- Сохранение результатов анализа резюме
- Кэширование данных пользователя
- Хранение метаданных резюме

### AI‑аналитика
- Интеграция с Claude AI для анализа резюме
- Генерация персональных рекомендаций
- ATS‑оценка резюме

### Аутентификация
- Вход через Puter Auth
- Управление сессиями пользователя

**Конфигурация:** Все настройки Puter находятся в `app/lib/puter.ts`

---

## 📁 Структура проекта

```
ai-resume-analyzer/
├── app/
│   ├── components/          # React компоненты
│   │   ├── Accordion.tsx
│   │   ├── ATS.tsx          # Компонент ATS‑оценки
│   │   ├── FileUploader.tsx # Загрузка файлов
│   │   ├── LanguageSwitcher.tsx
│   │   ├── Navbar.tsx       # Навигация и шапка
│   │   ├── resume-preview/  # Предпросмотр резюме
│   │   │   ├── ResumePreview.tsx
│   │   │   └── styles/      # Стили резюме (Classic, Minimal, Modern)
│   │   └── resume-wizard/   # Мастер создания резюме
│   │       ├── ResumeWizard.tsx
│   │       └── Step*.tsx     # Шаги визарда
│   ├── lib/                 # Утилиты и библиотеки
│   │   ├── i18n.ts          # Инициализация i18next
│   │   ├── language.ts      # Управление языками
│   │   ├── pdf2img.ts       # Конвертация PDF → изображение
│   │   ├── puter.ts         # Интеграция с Puter
│   │   ├── resume-ai.ts     # AI‑анализ резюме
│   │   ├── resume-store.ts  # Хранилище резюме
│   │   └── utils.ts         # Вспомогательные функции
│   ├── routes/              # Страницы приложения
│   │   ├── auth.tsx         # Страница аутентификации
│   │   ├── home.tsx         # Главная страница (список резюме)
│   │   ├── upload.tsx       # Загрузка и анализ резюме
│   │   ├── resume.tsx       # Страница с результатом анализа
│   │   ├── resume-preview.tsx # Предпросмотр резюме
│   │   └── create.tsx       # Создание резюме через визард
│   ├── root.tsx             # Корневой компонент
│   └── app.css              # Глобальные стили
├── constants/               # Константы и промпты
│   ├── index.ts
│   └── resume-prompts.ts    # Промпты для AI
├── locales/                 # Файлы переводов
│   ├── en.json
│   └── ru.json
├── public/                  # Статические файлы
│   ├── icons/               # SVG иконки
│   └── images/              # Изображения и GIF
├── types/                   # TypeScript типы
│   ├── index.d.ts
│   └── puter.d.ts
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── vite.config.ts
└── react-router.config.ts
```

---

## 🔄 Сценарий работы

### Анализ существующего резюме

1. **Аутентификация** — пользователь входит через Puter Auth
2. **Загрузка PDF** — пользователь загружает PDF‑файл резюме через drag & drop
3. **Конвертация** — приложение конвертирует PDF в изображение с помощью `pdfjs-dist`
4. **AI‑анализ** — отправка запроса к Claude AI с локализованной инструкцией
5. **Сохранение** — результат сохраняется в KV‑хранилище Puter
6. **Отображение** — показ карточки резюме с оценками ATS и рекомендациями

### Создание нового резюме

1. **Запуск визарда** — пользователь переходит на страницу создания резюме
2. **Заполнение данных** — пошаговое заполнение информации:
   - Заголовок и профессия
   - О себе (About)
   - Опыт работы (Experience)
   - Навыки (Skills)
   - Языки (Languages)
   - Рекомендации (Recommendations)
3. **AI‑генерация** — автоматическая генерация текстов через AI
4. **Предпросмотр** — выбор стиля оформления и предпросмотр
5. **Сохранение** — сохранение готового резюме

---

## ⚙️ Переменные окружения

В зависимости от конфигурации Puter могут потребоваться дополнительные настройки. Проверьте документацию Puter для получения токенов и ключей доступа.

Все настройки интеграции находятся в `app/lib/puter.ts`.

---

## 🐛 Решение проблем

### Ошибка AI 400 (Permission denied / usage-limited-chat)

**Проблема:** Ограничение со стороны AI‑делегата Puter.

**Решение:**
- Проверьте лимиты использования AI в вашем аккаунте Puter
- Убедитесь, что у вас есть доступ к AI‑функциям
- Приложение отображает понятное сообщение об ошибке и останавливает загрузку

### Большой GIF на странице загрузки

**Проблема:** GIF занимает слишком много места.

**Решение:** GIF отцентрирован и ограничен по ширине через Tailwind‑классы. При необходимости измените `max-w-*` класс в компоненте.

### Проблемы с локализацией

**Проблема:** Переводы не отображаются корректно.

**Решение:**
- Проверьте, что файлы переводов (`en.json`, `ru.json`) существуют в `locales/`
- Убедитесь, что `i18n.ts` правильно инициализирован
- Проверьте ключи переводов в компонентах

---

## 📝 Лицензия

MIT License

---

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста, создавайте issues и pull requests.

---

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

**Сделано с ❤️ используя React, TypeScript и Puter**
